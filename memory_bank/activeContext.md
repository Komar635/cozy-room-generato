# Active Context

## Текущий фокус
**Дата обновления:** 2026-04-16

### Активные задачи
1. ✅ Актуализирован Memory Bank после аудита репозитория.
   - Зафиксированы новые тесты, документация, CI/CD и рабочие паттерны.
   - Уточнено состояние AI, 3D-оптимизаций, сохранения проектов и demo-страниц.

2. ✅ Закрыты первые три подпункта блока UI/UX из плана tasks.
   - Убран flash темы при первом рендере и доведены theme tokens в основных UI-компонентах.
   - Добавлены индикаторы для фоновой загрузки 3D моделей и admin async-flow.
   - Улучшена мобильная компоновка экрана `/room` и связанных панелей.

3. ✅ Полностью закрыт блок 13 UI/UX и адаптивности.
   - Добавлены дополнительные motion-эффекты для landing/header/workspace/catalog.
   - Подтверждено, что keyboard shortcuts уже были реализованы ранее и фактически соответствуют подпункту 13.5.

4. ✅ Подпункт 15.3 по E2E доведен до минимально достаточного состояния.
   - Стабилизирован сценарий Playwright для `/api-demo` за счет явных test ids и устранения хрупких текстовых/role-селекторов.
   - Исправлены пользовательские тексты с mojibake в `app/api-demo/page.tsx`.
   - Добавлены базовые E2E сценарии для создания комнаты, применения стиля/локального сохранения и AI demo-flow.

5. ✅ Подпункт 15.4 по 3D performance закрыт practical minimum-ом.
   - Добавлена страница `/room/performance` с baseline fixture, автоматическим orbit capture и живыми метриками FPS/frame-time/heap.
   - Добавлен воспроизводимый сбор evidence через `npm run perf:3d` с сохранением JSON в `docs/perf-evidence/3d-performance-baseline.json`.
   - `docs/3D_PERFORMANCE_BASELINE.md` обновлен конкретным workflow, таблицей метрик и критериями прохождения.

6. ✅ Подпункт 15.5 по browser/device testing закрыт practical minimum-ом.
   - Сохранена существующая Playwright matrix: `chromium` / `firefox` / `webkit` / `mobile-chrome` / `mobile-safari`.
   - Добавлена воспроизводимая команда `npm run test:e2e:matrix` для прогона основного smoke spec на всей матрице.
   - Локальная верификация `2026-04-16`: `15/15 passed` на `e2e/room-designer.spec.ts`.

7. ✅ Блок 15 тестирования закрыт в формате practical minimum.
   - Unit и integration тесты проходят стабильно.
   - E2E, 3D performance и browser/device verification теперь имеют воспроизводимые команды и evidence.

## Текущие приоритеты
1. Переключиться на блок 16 и выбрать следующий минимально достаточный подпункт.
2. При расширении E2E coverage поддерживать `test:e2e:matrix` как честный smoke gate для браузеров и устройств.
3. При следующей итерации точечно чистить mojibake в AI route handlers, если это будет включено в scope.

## Известные блокеры
- В `components/ai/image-generator.tsx` уже существовали lint errors с неэкранированными кавычками; они не связаны с текущей итерацией и мешают полностью зеленому `npm run lint`.
- Постоянное серверное хранение проектов не завершено: `/api/room/save` сохраняет/возвращает данные, но загрузка по ID остается заглушкой.

## Следующие шаги
1. При переходе к блоку 16 при необходимости вынести matrix smoke в CI.
2. Расширять browser/device smoke только вместе с ростом основных E2E сценариев.
3. Отдельно не трогать legacy lint issues вне scope.
